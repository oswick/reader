package com.example.myapp

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // No need for custom method channel since we're using permission_handler package
}